import styled from "@suid/system/styled";

export namespace S {
    export const LoaderWrapper = styled('div')({
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '15%'
    });
}
